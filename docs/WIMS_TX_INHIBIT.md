# Moved

TX Inhibit and KEY agent design for this repository live in:

**[TX_INHIBIT.md](TX_INHIBIT.md)**

That document is self-contained. A KEY agent senses a priority radio’s KEY
and sends UDP hold/keepalive/release hold to each WSJT-X station.
Product-specific multi-op frameworks are optional clients of the same protocol.

**Operators — shared USB CAT + RTS/DTR (common pitfalls):**  
see [TX_INHIBIT.md § Shared USB CAT + RTS/DTR](TX_INHIBIT.md#shared-usb-cat--rtsdtr).
